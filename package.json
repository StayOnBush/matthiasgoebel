{
  "name": "@example/minimal",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "watch:sass": "sass --no-source-map --watch src:src",
    "build:sass": "sass --no-source-map src:src",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^0.22.20",
    "sass": "^1.49.7",
    "snowpack": "^3.8.8"
  }
}
